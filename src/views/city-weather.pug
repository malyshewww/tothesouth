extends ./layouts/default

block variables 
	- let pageTitle = "Алушта";
	- let breadCrumbsList = [{"title": "Главная", "link": "/"}, {"title": "Крым", "link": "/"}];
	- let currentPage = "Алушта"

append styles 
	link(href="/assets/styles/city-weather.css" rel="stylesheet")
	link(href="/assets/styles/weather-widget.css" rel="stylesheet")

block content 
	.city-weather
		.container 
			.page-title= pageTitle
			+cityNavigation(5)
			.heading 
				.heading__title.title-xs Погода
			.city-weather__body 
				include ./components/weather-widget
